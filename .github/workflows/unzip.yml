name: Auto-unzip uploaded archive

# Triggers automatically whenever a .zip is pushed to the repo root,
# or you can run it manually from the Actions tab.
on:
  push:
    paths:
      - '**/*.zip'
  workflow_dispatch:
    inputs:
      zip_path:
        description: 'Path to the zip file in the repo (leave blank to auto-detect)'
        required: false
        default: ''

permissions:
  contents: write

jobs:
  unzip:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Find the zip file
        id: find
        run: |
          if [ -n "${{ github.event.inputs.zip_path }}" ]; then
            ZIP="${{ github.event.inputs.zip_path }}"
          else
            ZIP=$(find . -maxdepth 2 -name "*.zip" ! -path "./node_modules/*" | head -n 1)
          fi
          if [ -z "$ZIP" ]; then
            echo "No zip file found in the repo." >&2
            exit 1
          fi
          echo "zip=$ZIP" >> "$GITHUB_OUTPUT"
          echo "Found: $ZIP"

      - name: Extract (skips .github/workflows — the default token can't write there)
        run: |
          ZIP="${{ steps.find.outputs.zip }}"
          TMP=$(mktemp -d)
          unzip -q "$ZIP" -d "$TMP"

          # If the archive contains a single top-level folder (e.g. "matchpulse/"),
          # flatten it so the project files land at the repo root instead of
          # being nested one level deep.
          ENTRIES=$(ls "$TMP")
          COUNT=$(echo "$ENTRIES" | wc -l)
          if [ "$COUNT" -eq 1 ] && [ -d "$TMP/$ENTRIES" ]; then
            SRC="$TMP/$ENTRIES"
          else
            SRC="$TMP"
          fi

          # Copy everything except .github/workflows/** — GitHub blocks the
          # default GITHUB_TOKEN from creating/modifying workflow files.
          rsync -a --exclude='.github/workflows/**' "$SRC"/ .

          rm -f "$ZIP"
          rm -rf "$TMP"

      - name: Commit extracted files
        run: |
          git config user.name "auto-unzip-bot"
          git config user.email "actions@github.com"
          git add -A
          git diff --cached --quiet && echo "Nothing to commit" || git commit -m "chore: auto-extract uploaded archive"
          git push
